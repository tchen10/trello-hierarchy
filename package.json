{
  "name": "trello-hierarchy",
  "version": "0.1.0",
  "description": "A Trello Power-Up for creating parent/child relationships between cards",
  "dependencies": {
    "eslint": "^4.18.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
